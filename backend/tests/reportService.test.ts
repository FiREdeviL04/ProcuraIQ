import test from 'node:test'
import assert from 'node:assert/strict'

test.skip('dashboard report aggregates expected shapes', () => {
  // TODO: wire Prisma mocking and assert totals/spend trend shapes.
  assert.ok(true)
})

test.skip('vendor performance report returns top vendor metrics', () => {
  // TODO: mock vendor quotations and approvals.
  assert.ok(true)
})
